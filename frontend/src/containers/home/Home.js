import { Carousel, Container, Row, Col } from "react-bootstrap";
import ReactPlayer from "react-player";

const Home = () => {
  return (
    <main>
      <Container>
        {/* https://www.testynaprawko.pl/ w tle obrazek podony jak tutaj, na nim centered przyciski */}
        <select name="" id="">
          <option value="b">b</option>
        </select>
        <select name="" id="">
          <option value="znaki zakazu">znaku zakazu</option>
        </select>
        <button>Przeglądaj pytania</button>
        <button>Rozpocznij naukę</button>
      </Container>
    </main>
  );
}

export default Home;